%h1 Edit member password

- form_for :member, :url => update_password_admin_member_path, :html => { :method => :put }  do |f|

  %fieldset
    %legend 
      = "#{@member.name}, #{@member.company_name}"
    
    %p
      %label{:for => "member_password"} Admin Password:
      = f.password_field :password
    
    %p
      %label{:for => "member_password_confirmation"} Admin Password Again:
      = f.password_field :password_confirmation
  
  %p.buttons
    = f.submit 'Update'
    or
    = link_to 'Cancel', edit_admin_member_path(@member.id)
