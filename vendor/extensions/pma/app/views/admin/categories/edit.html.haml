%h1 Edit category

- form_for :category, :url => category_path, :html => { :method => :put, :multipart => 'true' }  do |f|
  = render :partial => "form", :locals => {:f => f, :submit_label => "Update"}
